# 🏠 InSeoul — 10주 스프린트 계획

> **프로젝트 기간**: 2026년 3월 ~ 5월 (10주)  
> **스프린트 단위**: 1주 (월~금)  
> **기준 문서**: InSeoul 프로젝트 개발 기획서 v1.0

---

## 📋 전체 로드맵 요약

| 단계 | 기간 | 목표 |
|------|------|------|
| **Phase 1** — MVP 프론트엔드 | 1~4주차 | 핵심 화면 5개 구현 + 계산 로직 완성 |
| **Phase 2** — 데이터 연동 | 5~6주차 | 공공 API + 실제 지도 연동 |
| **Phase 3** — 고도화 & 완성 | 7~10주차 | 안정화, 성능 최적화, 배포 |

---

## Phase 1 — MVP 프론트엔드 (1~4주차)

### 🗓️ 1주차 — 프로젝트 셋업 & 공통 컴포넌트

**목표**: 개발 환경 구성 및 공통 UI 기반 완성

| 분류 | 세부 작업 |
|------|----------|
| **환경 구성** | Vite + React 18 프로젝트 초기화 |
| | Tailwind CSS v3 (PostCSS) 설정 |
| | React Router v6 라우팅 구조 설정 (`/`, `/dashboard`, `/report`, `/map`, `/strategy`) |
| | Zustand 또는 React Context 전역 상태 설계 |
| | ESLint / Prettier / 절대 경로 별칭 설정 |
| **공통 컴포넌트** | `Header` 네비게이션 (로고, 탭, 다크모드 토글, 햄버거 메뉴) |
| | 디자인 토큰 정의 (Primary `#a2d2ff`, 배경, 상태 색상 등) |
| | Noto Sans KR 폰트, Material Symbols 아이콘 연결 |
| | 다크모드 class 기반 토글 구현 |
| **기타** | 폴더 구조 확정 (`pages/`, `components/`, `hooks/`, `store/`, `utils/`) |

**완료 기준 (DoD)**
- [x] `npm run dev` 실행 시 5개 라우트가 정상 렌더링
- [x] Header 컴포넌트 라이트/다크 모드 모두 동작
- [x] 전역 상태 스토어에 `UserProfile` / `SimulationConfig` 슬라이스 존재

**✅ 1주차 완료 (2026-03-06)**

---

### 🗓️ 2주차 — 데이터 입력 화면 + 골든크로스 대시보드

**목표**: 사용자 입력 흐름 및 핵심 시각화 완성

| 분류 | 세부 작업 |
|------|----------|
| **DataInputScreen (`/`)** | 3단계 Step Wizard UI 구현 |
| | 상단 스텝 인디케이터 + 프로그레스 바 (33% → 66% → 100%) |
| | `+1,000만` / `+5,000만` / `+1억` 빠른 입력 버튼 |
| | React Hook Form + Zod 입력 유효성 검증 |
| | 스마트 팁 사이드 패널 (30대 직장인 평균 통계) |
| | 개인정보 보호 배너 |
| | Step 3 완료 시 `/dashboard` 이동 + 전역 상태 저장 |
| **DashboardScreen (`/dashboard`)** | SVG / Recharts 골든크로스 라인 차트 (자산 곡선 vs 아파트 가격 곡선) |
| | D-Day 배너 (예상 교차점 날짜 표시) |
| | 시뮬레이션 슬라이더 패널 (저축 증가율 0~30%, 투자 수익률 -10~30%) |
| | LTV 대출 가능액 / 필요 자본금 자동 계산 표시 |
| **계산 로직** | 골든크로스 예측 함수 구현 (`utils/calculator.js`) |
| | LTV 50% 대출 계산 / 취득세 3.5% 계산 |

**완료 기준 (DoD)**
- [x] 3단계 입력 완료 시 데이터가 전역 상태에 저장되고 `/dashboard` 이동
- [x] 골든크로스 차트가 슬라이더 onChange 즉시 실시간 업데이트 (isAnimationActive=false)
- [x] D-Day 날짜가 계산 로직에 맞게 동적으로 변경
- [x] React Hook Form + Zod 인라인 유효성 검증 (alert 제거)
- [x] 이전 단계 버튼 (Step 2, 3)
- [x] 저축 증가율이 차트 모델에 즉시 반영 (effectiveProfile)
- [x] 정책 대출 상품 섹션 (보금자리론·디딤돌·버팀목)
- [x] /report → /strategy?type=relay|downsize 파라미터 전달
- [x] StrategyDetailScreen 탭 스위처 (경유/평수 하향)
- [x] StrategyDetailScreen KPI 전역 상태 연동

**✅ 2주차 완료 (2026-03-06)**

---

### 🗓️ 3주차 — 리스크 리포트 + 전략 상세 화면

**목표**: 분석 결과 제공 화면 완성

| 분류 | 세부 작업 |
|------|----------|
| **ReportScreen (`/report`)** | 리스크 컨트롤 타워 — 스트레스 테스트 카드 (금리 1% 상승 / 주택가격 10% 상승 시나리오) |
| | 리스크 D-Day 재계산 로직 연동 |
| | 징검다리 리포트 — 대안 전략 카드 2종 (경유 전략, 평수 하향 조정) |
| | 액션 가이드 배너 (월 저축액 증가 시 단축 기간 안내) |
| | 전략 카드 클릭 시 `/strategy` 이동 |
| **StrategyDetailScreen (`/strategy`)** | 전략 요약 카드 3종 KPI (소요 기간 / 필요 자본금 / 기대 수익률) |
| | 단계별 실행 가이드 타임라인 (자금 확보 → 임장 → 금융 상품 매칭) |
| | 정책 대출 상품 정보 표시 (보금자리론, 디딤돌 등) |

**완료 기준 (DoD)**
- [x] 리스크 시나리오 2종의 지연 일수가 계산 로직과 일치
- [x] 징검다리 카드 클릭 시 해당 전략 상세 페이지 정상 렌더링
- [x] 모든 텍스트 정보가 전역 상태의 사용자 데이터를 반영

**✅ 3주차 완료 (2026-03-06)**

---

### 🗓️ 4주차 — 타겟팅 맵 + 반응형 최적화

**목표**: 인터랙티브 지도 및 모바일 반응형 완성

| 분류 | 세부 작업 |
|------|----------|
| **MapScreen (`/map`)** | SVG 기반 서울 모의 버블 맵 구현 |
| | 진입 가능성 3단계 버블 색상 (🟢 초록 / 🟡 노랑 / 🟣 보라) |
| | 버블 클릭 시 툴팁 (D-Day 카운터, 부족 금액, 대표 단지 가격) |
| | SVG 대시 화살표 징검다리 경로 표시 |
| | 평형대 / 연식 필터 기능 |
| | 줌 인/아웃 버튼 |
| **반응형 대응** | 모바일 (`< 768px`) 단일 컬럼, 햄버거 메뉴, 바텀 슬라이드 필터 |
| | 태블릿 (`768px ~ 1024px`) 2컬럼 레이아웃 |
| | 데스크탑 (`> 1024px`) 3컬럼 대시보드, 사이드바 고정 |
| **QA** | Chrome / Edge / Safari / Firefox 최신 2버전 크로스 브라우저 테스트 |
| | LCP 3초 이내 달성 확인 |

**완료 기준 (DoD)**
- [x] 서울 25개 구 버블 렌더링 및 색상 단계 정상 표시
- [x] 모바일에서 바텀 슬라이드 필터 동작
- [x] 전 화면 모바일/태블릿/데스크탑 레이아웃 정상

**✅ 4주차 완료 (2026-03-06)**

### 4주차 주요 구현 내역
- `MapScreen.jsx`: 서울 25개 구 전체 데이터 추가 (ALL_REGIONS 배열, 위치·시세·tier)
- 사용자 자산(cash, LTV) 기반 동적 tier 계산 (`calcGoldenCross` 연동)
- 평형대(×1.0/1.35/1.75) × 연식(×1.2/1.0) 배수 필터 실시간 반영
- 모바일 바텀 시트 필터 패널 + 드래그 핸들 CSS (`.bottom-sheet-handle`)
- 줌 인/아웃/초기화 버튼 개선
- 지역 클릭 시 사이드바 상세 + 하단 배너 실시간 업데이트
- 현황 요약 (즉시/3년내/장기 목표 구 개수 표시)
- tier 계산 버그 수정: `monthlySavings === 0`이면 `days = 99999`로 장기 처리
- `index.css`: `animate-fade-in` 키프레임, 바텀 시트 핸들 스타일 추가



## Phase 2 — 데이터 연동 (5~6주차)

### 🗓️ 5주차 — 공공 데이터 API 연동

**목표**: 실제 아파트 시세 데이터 자동 반영

| 분류 | 세부 작업 |
|------|----------|
| **API 연동** | 국토교통부 실거래가 공공 API 키 발급 및 연동 |
| | 구별 아파트 대표 시세 자동 업데이트 파이프라인 구축 |
| | 데이터 정규화 / 오류 처리 로직 구현 |
| **데이터 레이어** | API 응답 캐싱 (LocalStorage 또는 React Query) |
| | Mock 데이터 → 실제 API 데이터 전환 |
| | 데이터 갱신 주기 정책 결정 (일별 / 주별) |
| **계산 로직 고도화** | 실거래가 기반 아파트 가격 트렌드 모델 재계산 |
| | 골든크로스 예측 정확도 개선 |

**완료 기준 (DoD)**
- [x] 실제 API 연동 시 구별 가격 데이터 정상 표시
- [x] API 오류 발생 시 Mock 데이터로 fallback 처리
- [x] 캐시된 데이터로 오프라인에서도 기본 동작 가능

**✅ 5주차 완료 (2026-03-06)**

### 5주차 주요 구현 내역
- `src/services/api.js`: 국토교통부 아파트 실거래가 API 연동
- 보수적 접근으로 최근 3개월 데이터 합산 및 평균 계산 (`fetchAveragePriceForRegion`)
- 25개 구 전체 데이터 순차 조회 및 지연 발생 (`API_DELAY_MS`)을 통한 트래픽 제약을 우회
- `Zustand`와 연동하여 응답 데이터를 캐시 처리(`apiPrices`), 24시간 동안 재요청 방지
- MapScreen 진입 시 API 데이터를 우선 사용하며 `Loading Overlay` 적용
- 실제 데이터를 활용하여 `utils/calculator.js`와 함께 동적으로 아파트 시세 계산 연동
- `법정동코드 전체자료.txt`에서 스크립트를 통해 서울 25개 구 코드(LAWD_CD)만 상수화 (`utils/regionCodes.js`)

---

### 🗓️ 6주차 — 실제 지도 API 연동

**목표**: SVG 모의 맵 → 실제 서울 지도 전환

| 분류 | 세부 작업 |
|------|----------|
| **지도 연동** | Kakao Maps API 또는 Naver Maps API 키 발급 |
| | 실제 서울 구 경계(GeoJSON) 표시 |
| | 버블 마커를 실제 지도 좌표 기반으로 전환 |
| | 클릭 이벤트 / 툴팁을 지도 SDK와 통합 |
| | 헤더 검색창 — 지도 페이지 전용 지역 검색 기능 구현 |
| **MapScreen 고도화** | 줌 레벨에 따른 마커 크기 동적 조절 |
| | 모바일에서 터치 제스처(핀치 줌) 지원 |

**완료 기준 (DoD)**
- [x] 실제 서울 지도에 25개 구 버블 마커 표시
- [x] 지역 검색 시 해당 구로 지도 이동
- [x] SVG 모의 맵과 동일한 UX(클릭 툴팁, 경로 표시) 구현

**✅ 6주차 완료 (2026-03-06)**

### 6주차 주요 구현 내역
- `index.html`: Kakao Maps SDK 스크립트 추가 (`%VITE_KAKAO_MAP_KEY%` 활용)
- `MapScreen.jsx`: `react-kakao-maps-sdk` 연동하여 `Map`, `CustomOverlayMap`, `Polyline` 컴포넌트로 실제 지도 구성
- `ALL_REGIONS` 데이터에 25개 구 중심 좌표(`lat`, `lng`) 적용
- 헤더 검색창 입력 시 해당 구로 포커스 이동 (`center`, `level` 상태 관리)
- 기존 마커 버블 및 말풍선 툴팁 디자인 그대로 유지
- 징검다리 경로 `<Polyline>` 으로 대체하여 시각적 연속성 제공

---

## Phase 3 — 고도화 & 완성 (7~10주차)

### 🗓️ 7주차 — 백엔드 서버 구축 (선택적)

**목표**: 서버 연동 기반 마련 (사용자 저장 기능)

| 분류 | 세부 작업 |
|------|----------|
| **백엔드 셋업** | Spring Boot 또는 FastAPI 프로젝트 초기화 |
| | PostgreSQL / MySQL 데이터베이스 스키마 설계 |
| | 사용자 시나리오 저장 REST API 설계 및 구현 |
| **인증** | 소셜 로그인 또는 간단한 회원가입/로그인 구현 |
| | Header 사용자 아이콘 → 마이페이지 연결 |
| **프론트 연동** | LocalStorage → 서버 저장 전환 (선택) |
| | 복수 시나리오 저장/조회 기능 |

**완료 기준 (DoD)**
- [ ] 로그인 후 시나리오 저장 및 불러오기 동작
- [ ] REST API 기본 CRUD 동작 확인

---

### 🗓️ 8주차 — 성능 최적화 & 접근성

**목표**: 품질 지표 달성 및 사용자 경험 개선

| 분류 | 세부 작업 |
|------|----------|
| **성능 최적화** | Vite 번들 분석 및 청크 스플리팅 |
| | 차트 라이브러리 경량화 (필요 시 교체) |
| | 이미지 / 아이콘 레이지 로딩 적용 |
| | LCP 3초 이내, 슬라이더 반응 100ms 이내 목표 달성 |
| **접근성** | ARIA 레이블 전반 추가 |
| | 키보드 포커스 스타일 표준화 |
| | 터치 타겟 최소 44px 보장 |
| **법적 고지** | 투자 자문 면책 고지 배너 구현 |
| | 개인정보 처리 방침 페이지 추가 |

**완료 기준 (DoD)**
- [ ] Lighthouse 성능 점수 80점 이상
- [ ] WCAG AA 수준 접근성 기본 준수
- [ ] 면책 고지 모든 주요 화면에 표시

---

### 🗓️ 9주차 — 단위 테스트 & 통합 테스트

**목표**: 핵심 계산 로직 및 UI 안정성 검증

| 분류 | 세부 작업 |
|------|----------|
| **단위 테스트** | `calculator.js` 골든크로스 예측 함수 테스트 |
| | LTV 계산 / 취득세 계산 단위 테스트 |
| | 리스크 스트레스 테스트 로직 검증 |
| **컴포넌트 테스트** | React Testing Library로 주요 컴포넌트 테스트 |
| | Step Wizard 단계 전환 플로우 테스트 |
| **E2E 테스트** | Playwright 또는 Cypress로 핵심 사용자 여정 테스트 |
| | 데이터 입력 → 대시보드 → 리포트 → 맵 전체 플로우 |

**완료 기준 (DoD)**
- [ ] 계산 로직 단위 테스트 커버리지 80% 이상
- [ ] E2E 핵심 플로우 테스트 통과

---

### 🗓️ 10주차 — 배포 & 최종 QA

**목표**: 프로덕션 배포 및 최종 점검

| 분류 | 세부 작업 |
|------|----------|
| **배포** | Vercel 프론트엔드 배포 (CI/CD 파이프라인 구성) |
| | HTTPS 인증서 확인 |
| | 백엔드 AWS EC2 배포 (7주차 진행 시) |
| | 환경 변수 관리 (API 키, DB 접속 정보 분리) |
| **최종 QA** | Chrome / Edge / Safari / Firefox 크로스 브라우저 최종 검증 |
| | 모바일 실기기 테스트 (iOS Safari, Android Chrome) |
| | API 에러 핸들링 / 엣지 케이스 점검 |
| **문서화** | README.md 작성 (설치, 실행, 환경 변수 가이드) |
| | API 명세서 작성 (백엔드 연동 시) |
| | 서비스 소개 데모 영상 녹화 |

**완료 기준 (DoD)**
- [ ] 프로덕션 URL 접속 정상 동작
- [ ] 전 브라우저 / 모바일 최종 이상 없음
- [ ] README 및 데모 영상 완성

---

## 📊 주차별 진행 요약

```
Week  1  ██████████  [완료] 환경 셋업 + Header + 전체 화면 기초 구현
Week  2  ██████████  [완료] RHF+Zod 유효성·실시간 슬라이더·전략 상세 연동
Week  3  ██████████  [완료] 리포트 고도화 + 전략 상세 완성
Week  4  ██████████  [완료] 서울 25개 구 타겟팅 맵 + 반응형 최적화
Week  5  ██████████  [완료] 공공 API 데이터 연동
Week  6  ██████████  [완료] 실제 지도 API 연동
Week  7  ░░░░░░░░░░  백엔드 서버 (선택)
Week  8  ░░░░░░░░░░  성능 최적화 + 접근성
Week  9  ░░░░░░░░░░  테스트 작성 + 검증
Week 10  ░░░░░░░░░░  배포 + 최종 QA
```

---

## ⚠️ 주요 리스크 & 대응

| 리스크 | 대응 |
|--------|------|
| 지도 API 비용 초과 | 5~6주차까지 SVG 모의 맵 유지, 필요 시 무료 플랜 내 트래픽 제한 |
| 공공 API 데이터 품질 | Mock 데이터 fallback 유지, 데이터 정규화 로직 선 구현 |
| 금융 계산 오류 | 9주차 단위 테스트에서 수치 검증, 면책 고지 필수 |
| 7주차 백엔드 일정 지연 | Phase 1~2 완성 후 선택적 추가 — Phase 3 고도화 우선순위 조정 가능 |
